<config>
        <param name="local.databasevalue">RICE_241</param>
        <param name="application.url">http://localhost:8080</param>
        <param name="kr.url">${application.url}/kr</param>
        <param name="datasource.username">${local.databasevalue}</param>
        <param name="datasource.password">${local.databasevalue}</param>
        <param name="datasource.url">jdbc:mysql://mysql:3306/${local.databasevalue}</param>
	<param name="krad.url">${application.url}/kr-krad</param>
        <param name="kdo.db.skip">false</param>
        <param name="project.db.reset">true</param>
        <param name="db.vendor">mysql</param>
        <param name="datasource.ojb.platform">MySQL</param>
</config>

